// This component is no longer used as the app is now YouTube-link only.
// Keeping file for reference or future re-integration if needed.
import React from 'react';

const FileUpload: React.FC = () => {
  return null;
};

export default FileUpload;